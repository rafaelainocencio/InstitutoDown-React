import { type } from "os";
import { useNavigate } from "react-router-dom";


type Props = {
    id: string
}

export default function FormCandidatar({id}:Props){
    
    const navigate = useNavigate();

    
}